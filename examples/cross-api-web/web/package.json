{
  "name": "cross-api-web-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Vue + TypeScript consumer side of the Orcho cross-project demo.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "vite --host 127.0.0.1",
    "build": "vue-tsc --noEmit && vite build"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "vite": "^7.1.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vue-tsc": "^3.0.0"
  }
}
